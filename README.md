Part Two of the JWKS Server Project

Added SQLite as a database, with keys being stored in said database.

To Run:

    1. Navigate to the same directory that the project files are contained in.
    2. In a terminal, type 'go run main.go'. 
        This will start the server.
    3. To view the Gradebot's results, open a separate terminal and run 
        './gradebot project2'

I utilized AI again to aid me in coding this project.
We had a womderful time throwing code past one another. That is, Gemini and I. 
The initial prompt was as follows: 

    I wish to create/open a sqlite file at the beginning of running my simple JWKS server to store private keys. 
    I am using VsCode, how would I go about doing this?


    

